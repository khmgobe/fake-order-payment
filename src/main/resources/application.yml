spring:
  jpa:
    hibernate:
      ddl-auto: create
    open-in-view: false
  datasource:
    url: jdbc:mariadb://localhost:3307/backend
    username: root
    password: root

logging:
  level:
    org.hibernate.sql: debug # logger